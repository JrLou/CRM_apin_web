// import { xxx } from '../services/xxx';
export default {
  namespace: "flyingpigDetail",
  state: {},
  effects: {
    *fetch({ payload }, { call, put }) {
    },
  },
  reducers: {
    save(state, action) {
      return {
        ...state,
      };
    },
  },
};

